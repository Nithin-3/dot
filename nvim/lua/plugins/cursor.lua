require('smear_cursor').setup({
    cursor_color = '#019606',
    -- particles_enabled = true,
    -- particle_max_num = 200,
    -- traling_exponent = 10,
    -- gradient_exponent = 2,
    -- damping = 0.6,
})
